/**
 * 构建标记:显示在首页最下面。
 *
 * 作用很实在 —— 排查问题时能一眼确认「手机上打开的到底是不是最新代码」。
 * 网页版部署在 GitHub Pages,浏览器和 Service Worker 都会缓存,
 * 常常出现「代码明明改了,手机上还是旧的」。有了这个标记就不用猜。
 *
 * 每次发新版本时把这里 +1。
 */
export const BUILD_TAG = 'w54'
